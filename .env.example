DATABASE_URL="postgresql://postgres:postgres@localhost:5432/event_ticketing?schema=public"

# NextAuth
AUTH_SECRET="dev-only-secret-change-me-please-generate-a-real-one"
NEXTAUTH_URL="http://localhost:3000"

# Public app URL used to build QR/ticket/registration links and email content
APP_URL="http://localhost:3000"

# SMTP is configured from the admin dashboard (Settings -> Email), not here —
# see the SmtpSettings table.

# Bootstrap admin (used by the seed script)
SEED_ADMIN_NAME="Admin"
SEED_ADMIN_EMAIL="admin@example.com"
SEED_ADMIN_PASSWORD="ChangeMe123!"
